<Page
    x:Class="SnapCaption.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnapCaption"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingPage"
    JournalEntry.KeepAlive="False"
    mc:Ignorable="d">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Column: Settings in vertical layout -->
        <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,20,0">
            
            <!-- Target Language -->
            <StackPanel Margin="0,0,0,15" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,0,5"
                        VerticalAlignment="Center"
                        Text="Target Language" />
                    <ui:Flyout x:Name="TargetLangInfoFlyout">
                        <TextBlock Width="350" TextWrapping="Wrap">
                            <Run FontWeight="DemiBold" TextDecorations="Underline" Text="&quot;There isn't the target language I expected!&quot;" />
                            <Run Text="&#x0A;You can directly edit the content of this combobox to customize the language, and it is recommended to follow the" />
                            <Run FontWeight="Bold" Text="BCP 47 language tag." />
                            <Run FontWeight="Bold" Text="&#x0A;&#x0A;Note:" />
                            <Run Text="Some of APIs (such as DeepL) needs another way to define target language, see their official docs for more details." />
                            <Run Text="&#x0A;No need to consider this for included target languages, since we've built in tag mappings. But if your expected language isn't in the list, keep this in mind." />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="TargetLangInfo"
                        Width="15"
                        Height="15"
                        Margin="5,0,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="TargetLangInfo_MouseEnter"
                        MouseLeave="TargetLangInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ComboBox
                    x:Name="TargetLangBox"
                    Width="150"
                    Height="30"
                    HorizontalAlignment="Left"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    IsEditable="True"
                    LostFocus="TargetLangBox_LostFocus"
                    SelectionChanged="TargetLangBox_SelectionChanged" />
            </StackPanel>

            <!-- Translate API -->
            <StackPanel Margin="0,0,0,15" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="0,0,0,5" Text="Translate API" />
                    <ui:Flyout x:Name="TranslateAPIInfoFlyout">
                        <TextBlock
                            Width="300"
                            Text="Except for Google and Google2, all other APIs require configuring before they can be used."
                            TextWrapping="Wrap" />
                    </ui:Flyout>
                    <Button
                        x:Name="TranslateAPIInfo"
                        Width="15"
                        Height="15"
                        Margin="5,0,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="TranslateAPIInfo_MouseEnter"
                        MouseLeave="TranslateAPIInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ComboBox
                    x:Name="TranslateAPIBox"
                    Width="150"
                    Height="30"
                    HorizontalAlignment="Left"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    SelectedItem="{Binding ApiName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectionChanged="TranslateAPIBox_SelectionChanged" />
            </StackPanel>


            <!-- API Interval -->
            <StackPanel Margin="0,0,0,15" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="0,0,0,5" Text="API Interval" />
                    <ui:Flyout x:Name="FrequencyInfoFlyout">
                        <TextBlock Width="300" TextWrapping="Wrap">
                            <Run Text="Determines the frequency of translate API calls. The smaller it is, the more frequent API calls." />
                            <Run Text="&#x0A;The translate API is called once after the caption changes" />
                            <Run FontWeight="SemiBold" Text="[API Interval]" />
                            <Run Text="times." />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="FrequencyInfo"
                        Width="15"
                        Height="15"
                        Margin="5,0,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="FrequencyInfo_MouseEnter"
                        MouseLeave="FrequencyInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <Slider
                    Width="150"
                    HorizontalAlignment="Left"
                    AutoToolTipPlacement="TopLeft"
                    AutoToolTipPrecision="0"
                    Maximum="10"
                    Value="{Binding MaxSyncInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>


            <!-- Debug Mode -->
            <StackPanel Margin="0,0,0,0" Orientation="Vertical">
                <TextBlock Margin="0,0,0,5" Text="Debug Mode" />
                <ui:ToggleSwitch
                    Width="100"
                    Height="30"
                    HorizontalAlignment="Left"
                    Padding="10,4,10,7"
                    IsChecked="{Binding MainWindow.DebugMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    OffContent="Off"
                    OnContent="On" />
            </StackPanel>
        </StackPanel>

        <!-- Right Column: Logo and Info -->
        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- SnapCaption Logo -->
            <Image
                x:Name="LogoImage"
                Width="150"
                Height="150"
                Margin="0,60,0,15"
                Source="pack://application:,,,/src/snapcaption.png"
                Stretch="Uniform" />

            <!-- Title Text -->
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                TextAlignment="Center"
                Margin="0,0,0,5"
                Text="SnapCaption" />

            <!-- Version -->
            <TextBlock
                FontSize="14"
                TextAlignment="Center"
                Margin="0,0,0,15"
                Text="v1.0.0" />

            <!-- Repository Link -->
            <TextBlock Margin="0,0,0,0" TextAlignment="Center">
                <Hyperlink NavigateUri="https://github.com/codafyrepo/SnapCaption" RequestNavigate="Hyperlink_RequestNavigate">
                    <Hyperlink.Style>
                        <Style TargetType="Hyperlink" BasedOn="{StaticResource {x:Type Hyperlink}}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#FF62AD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Hyperlink.Style>
                    <Run Text="https://github.com/codafyrepo/SnapCaption" />
                </Hyperlink>
            </TextBlock>
            
            <!-- Codafy Logo -->
            <Image
                x:Name="CodafyImage"
                Width="270"
                Height="270"
                Margin="0,-60,0,0"
                Source="pack://application:,,,/src/codafy.png"
                Stretch="Uniform" />
        </StackPanel>
    </Grid>
</Page>
