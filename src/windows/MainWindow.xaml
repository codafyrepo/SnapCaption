<ui:FluentWindow
    x:Class="SnapCaption.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnapCaption"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SnapCaption - v1.1.0"
    Width="900"
    Height="590"
    MinWidth="900"
    MinHeight="590"
    MaxWidth="900"
    MaxHeight="590"
    Background="Transparent"
    ExtendsContentIntoTitleBar="True"
    LocationChanged="MainWindow_LocationChanged"
    ResizeMode="NoResize"
    Topmost="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="Manual"
    WindowStyle="None"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="MainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:TitleBar
                Grid.Row="0"
                Height="27"
                ShowMaximize="False">
                <ui:TitleBar.Header>
                    <ui:TextBlock
                        Padding="15,0,0,0"
                        VerticalAlignment="Center"
                        Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type ui:FluentWindow}}}" />
                </ui:TitleBar.Header>
                <ui:TitleBar.TrailingContent>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <ui:Button
                            x:Name="NativeCaptionsButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="NativeCaptionsButton_Click"
                            Icon="{ui:SymbolIcon TextGrammarArrowLeft24,
                                                 Filled=False}"
                            ToolTip="Show/Hide Native LiveCaptions" />
                        <ui:Button
                            x:Name="OriginalCaptionButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="OriginalCaptionButton_Click"
                            Icon="{ui:SymbolIcon ChevronDown24,
                                                 Filled=True}"
                            ToolTip="Show/Hide Original Caption" />
                        <ui:Button
                            x:Name="SaveHistoryButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="SaveHistoryButton_Click"
                            Icon="{ui:SymbolIcon Save24,
                                                 Filled=False}"
                            ToolTip="Save History to File" />
                        <ui:Button
                            x:Name="CopyHistoryButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="CopyHistoryButton_Click"
                            Icon="{ui:SymbolIcon Copy24,
                                                 Filled=False}"
                            ToolTip="Copy History to Clipboard" />
                        <ui:Button
                            x:Name="ClearHistoryButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="ClearHistoryButton_Click"
                            Icon="{ui:SymbolIcon Broom24,
                                                 Filled=False}"
                            ToolTip="Clear History and Translated Caption" />
                        <ui:Button
                            x:Name="LogOnlyButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="LogOnlyButton_Click"
                            Icon="{ui:SymbolIcon Pause16,
                                                 Filled=False}"
                            ToolTip="Pause Translation (Log Only)" />
                        <ui:Button
                            x:Name="TopmostButton"
                            Margin="0,0,5,0"
                            Appearance="Transparent"
                            Click="TopmostButton_Click"
                            Icon="{ui:SymbolIcon Pin16,
                                                 Filled=True}"
                            ToolTip="Always on Top" />
                    </StackPanel>
                </ui:TitleBar.TrailingContent>
            </ui:TitleBar>

            <ui:NavigationView
                x:Name="RootNavigation"
                Grid.Row="1"
                Height="Auto"
                HeaderVisibility="Collapsed"
                IsBackButtonVisible="Collapsed"
                IsPaneOpen="False"
                IsPaneToggleVisible="False"
                PaneDisplayMode="LeftMinimal">
                <ui:NavigationView.MenuItems>
                    <ui:NavigationViewItem
                        Margin="0,6,0,3"
                        Content="Caption"
                        FontFamily="Bahnschrift"
                        Icon="{ui:SymbolIcon Home24}"
                        NavigationCacheMode="Enabled"
                        TargetPageType="{x:Type local:CaptionPage}" />
                    <ui:NavigationViewItem
                        Margin="0,3,0,0"
                        Content="Setting"
                        FontFamily="Bahnschrift"
                        Icon="{ui:SymbolIcon Settings24}"
                        NavigationCacheMode="Required"
                        TargetPageType="{x:Type local:SettingPage}" />
                </ui:NavigationView.MenuItems>
            </ui:NavigationView>
        </Grid>
        <ui:SnackbarPresenter x:Name="SnackbarHost" Grid.Row="0" />
        <ContentPresenter
            x:Name="DialogHostContainer"
            MinHeight="167"
            Visibility="Collapsed" />
    </Grid>
</ui:FluentWindow>
